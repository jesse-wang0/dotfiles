# dotfiles

## Setup

1. Clone repo to home directory:

```bash
git clone git@github.com:jesse-wang0/dotfiles.git ~/dotfiles
```

2. Run the symlink setup script:

```bash
chmod +x ~/dotfiles/scripts/symlink-setup.sh
~/dotfiles/scripts/symlink-setup.sh
```
